[project]
name = "pufferlib"
version = "4.0.0"
description = "Fast and sane reinforcement learning"
requires-python = ">=3.10"
readme = {file = "README.md", content-type = "text/markdown"}
license = {file = "LICENSE"}
authors = [{ name = "Joseph Suarez", email = "jsuarez@puffer.ai" }]
classifiers=[
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "setuptools",
    "numpy",
    "torch>=2.9",
    "rich",
    "rich_argparse",
    "gpytorch",
    "scikit-learn",
    "wandb",
    "pybind11",
]

[project.optional-dependencies]
test = [
    "pytest>=8",
    # Exact oracle versions used by the native Craftax parity suite.
    "jax==0.11.1",
    "craftax==1.5.0",
]

[project.scripts]
puffer = "pufferlib.pufferl:main"

[project.urls]
Homepage = "https://puffer.ai"

[tool.setuptools.packages.find]
where = ["."]
include = ["pufferlib*"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "cuda: requires an NVIDIA CUDA device and CUDA toolkit",
    "optional: requires optional research or environment dependencies",
]
